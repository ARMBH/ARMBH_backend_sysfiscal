- args:
    sql: CREATE TABLE "public"."documentos"("id" serial NOT NULL, "created_at" timestamptz
      NOT NULL DEFAULT now(), "name" text NOT NULL, "type" text NOT NULL, "size" text
      NOT NULL, "base64" text NOT NULL, "user_id" text NOT NULL, "processo_id" integer
      NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id")
      ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("processo_id") REFERENCES
      "public"."processos"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: documentos
    schema: public
  type: add_existing_table_or_view
