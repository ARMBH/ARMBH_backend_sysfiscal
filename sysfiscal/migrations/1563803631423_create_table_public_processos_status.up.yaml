- args:
    sql: CREATE TABLE "public"."processos_status"("id" serial NOT NULL, "status_id"
      integer NOT NULL, "processo_id" integer NOT NULL, "created_at" timestamptz NOT
      NULL DEFAULT now(), "due_date" timestamptz NOT NULL, "name" text, PRIMARY KEY
      ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: processos_status
    schema: public
  type: add_existing_table_or_view
