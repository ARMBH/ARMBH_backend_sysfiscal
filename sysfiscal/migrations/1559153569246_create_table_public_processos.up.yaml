- args:
    sql: CREATE TABLE "public"."processos"("id" serial NOT NULL, "title" text NOT
      NULL, "is_completed" boolean NOT NULL, "is_public" boolean NOT NULL, "created_at"
      timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz , "user_id" text
      NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: processos
    schema: public
  type: add_existing_table_or_view
